// mayBankVa
const linkajaEwallet = document.getElementById("linkajaEwallet");
const textNominallinkajaEwallet = linkajaEwallet.textContent; // biar presist

// 1
const linkajaEwallet_1 = document.getElementById("ml_starlight");
linkajaEwallet_1.addEventListener("click", () => {
  harga = "120.000";

  if (linkajaEwallet_1.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 2
const linkajaEwallet_2 = document.getElementById("ml_starlightPlus");
linkajaEwallet_2.addEventListener("click", () => {
  harga = "320.000";

  if (linkajaEwallet_2.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 3
const linkajaEwallet_3 = document.getElementById("ml_twilight");
linkajaEwallet_3.addEventListener("click", () => {
  harga = "140.500";

  if (linkajaEwallet_3.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 4
const linkajaEwallet_4 = document.getElementById("ml_86");
linkajaEwallet_4.addEventListener("click", () => {
  harga = "22.600";

  if (linkajaEwallet_4.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 5
const linkajaEwallet_5 = document.getElementById("ml_172");
linkajaEwallet_5.addEventListener("click", () => {
  harga = "45.800";

  if (linkajaEwallet_5.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 6
const linkajaEwallet_6 = document.getElementById("ml_257");
linkajaEwallet_6.addEventListener("click", () => {
  harga = "65.700";

  if (linkajaEwallet_6.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 7
const linkajaEwallet_7 = document.getElementById("ml_344");
linkajaEwallet_7.addEventListener("click", () => {
  harga = "85.600";

  if (linkajaEwallet_7.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 8
const linkajaEwallet_8 = document.getElementById("ml_429");
linkajaEwallet_8.addEventListener("click", () => {
  harga = "108.900";

  if (linkajaEwallet_8.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 9
const linkajaEwallet_9 = document.getElementById("ml_556");
linkajaEwallet_9.addEventListener("click", () => {
  harga = "138.760";

  if (linkajaEwallet_9.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 10
const linkajaEwallet_10 = document.getElementById("ml_706");
linkajaEwallet_10.addEventListener("click", () => {
  harga = "168.200";

  if (linkajaEwallet_10.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 11
const linkajaEwallet_11 = document.getElementById("ml_720");
linkajaEwallet_11.addEventListener("click", () => {
  harga = "170.800";

  if (linkajaEwallet_11.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 12
const linkajaEwallet_12 = document.getElementById("ml_878");
linkajaEwallet_12.addEventListener("click", () => {
  harga = "208.600";

  if (linkajaEwallet_12.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 13
const linkajaEwallet_13 = document.getElementById("ml_977");
linkajaEwallet_13.addEventListener("click", () => {
  harga = "235.400";

  if (linkajaEwallet_13.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 14
const linkajaEwallet_14 = document.getElementById("ml_1163");
linkajaEwallet_14.addEventListener("click", () => {
  harga = "278.800";

  if (linkajaEwallet_14.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 15
const linkajaEwallet_15 = document.getElementById("ml_1412");
linkajaEwallet_15.addEventListener("click", () => {
  harga = "332.860";

  if (linkajaEwallet_15.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 16
const linkajaEwallet_16 = document.getElementById("ml_4394");
linkajaEwallet_16.addEventListener("click", () => {
  harga = "978.500";

  if (linkajaEwallet_16.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 17
const linkajaEwallet_17 = document.getElementById("ml_5534");
linkajaEwallet_17.addEventListener("click", () => {
  harga = "1.248.100";

  if (linkajaEwallet_17.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});

// 18
const linkajaEwallet_18 = document.getElementById("ml_9302");
linkajaEwallet_18.addEventListener("click", () => {
  harga = "2.075.700";

  if (linkajaEwallet_18.checked) {
    linkajaEwallet.textContent = textNominallinkajaEwallet + "Rp " + harga;
    harga = 0; // set ulang biar g nimpa
  }
});
