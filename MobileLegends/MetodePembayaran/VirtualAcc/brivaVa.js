// Virtual Account
// let virtualAcc = document.getElementById('virtualAccount');

// brivaVa
const brivaVa = document.getElementById('brivaVa');
const textNominalbrivaVa = brivaVa.textContent; // biar presist

// 1
const briva_Va = document.getElementById('1');
briva_Va.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 2
const briva_Va2 = document.getElementById('2');
briva_Va2.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va2.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 3
const briva_Va3 = document.getElementById('3');
briva_Va3.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va3.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 4
const briva_Va4 = document.getElementById('4');
briva_Va4.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va4.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 5
const briva_Va5 = document.getElementById('5');
briva_Va5.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va5.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 6
const briva_Va6 = document.getElementById('6');
briva_Va6.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va6.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 7
const briva_Va7 = document.getElementById('7');
briva_Va7.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va7.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 8
const briva_Va8 = document.getElementById('8');
briva_Va8.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va8.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 9
const briva_Va9 = document.getElementById('9');
briva_Va9.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va9.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 10
const briva_Va10 = document.getElementById('10');
briva_Va10.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va10.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 11
const briva_Va11 = document.getElementById('11');
briva_Va11.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va11.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 12
const briva_Va12 = document.getElementById('12');
briva_Va12.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va12.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 13
const briva_Va13 = document.getElementById('13');
briva_Va13.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va13.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 14
const briva_Va14 = document.getElementById('14');
briva_Va14.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va14.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 15
const briva_Va15 = document.getElementById('15');
briva_Va15.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va15.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 16
const briva_Va16 = document.getElementById('16');
briva_Va16.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va16.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 17
const briva_Va17 = document.getElementById('17');
briva_Va17.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va17.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})

// 18
const briva_Va18 = document.getElementById('18');
briva_Va18.addEventListener('click', () => {
    harga += 117000;

    if (briva_Va18.checked) {
        brivaVa.textContent = textNominalbrivaVa + 'Rp.' + harga;
        harga = 0; // set ulang biar g nimpa 
    }
})